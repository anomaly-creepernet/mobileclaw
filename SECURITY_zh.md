# 安全政策

## 报告安全漏洞

如果你发现了安全漏洞，请提交带有 `security` 标签的 Issue。

不过——这是一个 AI 代理应用，它本身就会在用户设备上运行 Shell 命令、连接用户配置的 API 端点，并通过 Shizuku 获取提权。设计上，代理可以执行与其对话的 LLM 决定的任何操作。

所以这里的"漏洞"具体指：

- **远程代码执行** —— 让*第三方*在未经用户或代理主动发起的情况下执行设备上的代码。如果代理自己运行了命令，那是正常功能。
- **凭据泄露** —— API 密钥、令牌或身份/记忆文件内容暴露给非预期的第三方（例如以明文记录日志、发送到第三方服务器、其他应用可读取）。
- **意图劫持** —— 让代理执行用户未要求操作的方式（例如通过工具输出进行提示注入，而代理盲目信任了该输出）。

以下情况**不算**安全问题：
- 代理运行了你要求它运行的 Shell 命令
- 代理连接到了你配置的 API 端点
- 代理的 LLM 说了奇怪或错误的话
- Shizuku 授予了提权（用户是主动选择开启的）

## 支持的版本

仅支持 `master` 分支上的最新提交。
